# Private Function Tests

Place tests for helpers in `ConfluencePS/Private/*.ps1` in this directory.

Use one file per private helper named `<HelperName>.Tests.ps1`. Dot-source shared helpers with `../../Helpers/<HelperName>.ps1` from test files in this directory.
