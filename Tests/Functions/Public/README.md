# Public Function Tests

Place tests for cmdlets in `ConfluencePS/Public/*.ps1` in this directory.

Use one file per public cmdlet named `<CmdletName>.Tests.ps1`. Dot-source shared helpers with `../../Helpers/<HelperName>.ps1` from test files in this directory.
